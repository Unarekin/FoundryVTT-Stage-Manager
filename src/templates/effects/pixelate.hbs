<section>
  <input type="hidden" name="effect.id" id="effect.id" value="{{id}}">
  <input type="hidden" name="effect.type" id="effect.type" value="{{type}}">
  <input type="hidden" name="effect.serialized" id="effect.serialized" value="{{serialized}}">
  <div class="center">{{localize "STAGEMANAGER.EDITDIALOG.EFFECTS.PIXELATE"}}</div>

  <div class="form-group">
    <label for="effect.size">{{localize "STAGEMANAGER.EDITDIALOG.EFFECTS.CONFIG.PIXELATE.SIZE"}}</label>
    <input type="number" min="0" step="1" value="{{size}}" name="effect.size" id="effect.size">
  </div>

</section>